# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BeijingBus' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BeijingBus

end

target 'BeijingBus WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BeijingBus WatchKit App

end

target 'BeijingBus WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BeijingBus WatchKit Extension
  pod 'ObjectMapper'
  pod 'ChinaShift', :path => 'libs/ChinaShift'
end
